spring.datasource.url=jdbc:mysql://localhost:3306/employeedb?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Aasil123@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# disable Thymeleaf template cache for development
spring.thymeleaf.cache=false

# disable static resource caching so CSS/JS changes appear immediately
spring.resources.cache.period=0

# ensure devtools restarts on change (true by default)
spring.devtools.restart.enabled=true